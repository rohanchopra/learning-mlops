apiVersion: 1
# list of datasources to insert/update
# available in the database
datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    url: db:5439
    database: hw5
    user: postgres
    secureJsonData:
      password: 'temp'
    jsonData:
      sslmode: 'disable'
      database: hw5